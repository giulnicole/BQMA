% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_results.R
\name{add_annotation}
\alias{add_annotation}
\title{Attach probe classification onto a BQMAResult}
\usage{
add_annotation(object, array = c("450k", "EPIC"), snp_dist_problematic = 2L)
}
\arguments{
\item{object}{A \linkS4class{BQMAResult} object.}

\item{array}{Array type: \code{"450k"} or \code{"EPIC"}.}

\item{snp_dist_problematic}{Integer. See \code{\link[=annotate_results]{annotate_results()}}.}
}
\value{
The input \linkS4class{BQMAResult} object, with \verb{@probe_class}
populated.
}
\description{
Convenience writer: runs \code{\link[=annotate_results]{annotate_results()}} and copies the per-probe
classification onto the \verb{@probe_class} slot of the input object. The
slot value is a named character vector (one entry per probe), suitable
for display by the \code{show()} method.
}
\details{
Note that the classification is per-probe, not per (probe x tau) — it
is determined by the manifest, not by the model fit, so it does not
depend on \code{tau}.
}
\seealso{
\code{\link[=annotate_results]{annotate_results()}} for the full data.frame output.
}
